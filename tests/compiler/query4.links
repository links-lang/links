var db = database "my_database" "postgresql" "localhost:5432:links:12345";
var my_table = table "hello" 
	 with (word : String) from db ;

fun p(r) {true}

fun main() {
  for (r <-- my_table) 
  where (p(r))
  [r]
}

main()