fun test() {
  var lf = linfun(x) {x};
  switch (A(3)) {
    case A(a) -> ignore(lf(1)) # ; lf(2)
    case B(b) -> () # lf(3)
  }
}

# examples/linearity/switch.links:2: Type error: Variable lf has linear type
#     `(a::Any) -b-@ a::Any'
# but is used 4611686018427387903 times.
# In expression: var lf = linfun(x) {x};.