var db = database "timedb";

var timetbl = table "timetbl"
  with (foo: String) using transaction_time(tt_from, tt_to) from db;

var now = intToDate(serverTimeMilliseconds());
tt_insert timetbl values [(foo = "foo", tt_from = now)]
