(executable
  (name links)
  (public_name linx)
  (package links)
  (modes native)
  (libraries threads.posix dynlink ANSITerminal linenoise
   links.core links.frontend links.middleend links.backend links.runtime))
