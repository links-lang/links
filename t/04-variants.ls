(< label=2>) == (< label=2>);    # true : bool
(< label=2>) == (< bungle=2>);   # false : bool

if (case < label=2 > of < label=x > in x) == 2 then "success" else "failure";
case < label=2 > of < gumption=x > in x
