var db = database "test" "postgresql" ":5432:test:foobar";
var players_table = table "players" with (team : String, name : String, pos : String, eff : Int) from db;

query {
	sum(map(fun (r) { r.eff }, asList(players_table)))
}
