sudo: required
dist: trusty
language: c
services:
  - postgresql
cache:
  directories:
  - $HOME/.opam
before_install:
  - sudo add-apt-repository -y ppa:avsm/ppa
  - sudo apt-get -qq update
  - sudo apt-get install -y ocaml-nox ocaml-native-compilers camlp4-extra opam
  - opam init -a
  - opam switch 4.04.0
  - eval `opam config env`
  - opam update
  - opam upgrade -y
  - psql -c 'create database links;' -U postgres
install:
<<<<<<< HEAD
  - opam install -y deriving lwt postgresql sqlite3 mysql cgi base64 cohttp ANSITerminal
=======
  - opam install -y deriving lwt postgresql sqlite3 mysql cgi base64 cohttp websocket
>>>>>>> 2afa4577cdc1f05167d0d3a4eb92529b355509db
script:
  - make -j2 nc
  - make tests
  - ./run-tests db-only shredding
