sudo: required
dist: trusty
language: c
services:
  - postgresql
before_install:
  - sudo add-apt-repository -y ppa:avsm/ppa
  - sudo apt-get -qq update
  - sudo apt-get install -y ocaml-nox ocaml-native-compilers camlp4-extra opam
  - opam init -a
  - opam switch 4.04.0
  - eval `opam config env`
  - psql -c 'create database links;' -U postgres
install:
  - opam install -y postgresql
  - opam pin add links-core . -y
  - opam pin add links-postgresql . -y
  - opam pin add links . -y
  - opam pin remove links -y
  - opam pin remove links-postgresql -y
  - opam pin remove links-core -y
script:
  - make nc
  - make tests
  - ./run-tests db-only shredding
