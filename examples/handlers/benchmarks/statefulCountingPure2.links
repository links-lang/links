# Micro benchmark: Stateful counting (pure) 2
fun countPure(s) {
    var i = s(());
    if (i == 0) {
      i
    } else {
      countPure(fun(s) { i-1 })
    }
}

fun b(n) {
  var x = countPure(fun(s) { n });
  ()
}

b(10^7)