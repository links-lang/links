fun main() {
  fun giver(x,y,a) {
    (grab(give(y,give(x,request(a))))).1
  }

  fun grabber(a) {
    var s = accept(a);
    var (x,s) = grab(s);
    var (y,s) = grab(s);
    var _ = give(x+y,s);
    ()
  }

  var a = new();
  var _ = spawn { grabber(a) };
  giver(6,7,a)
}

main()
