var x = fuse(fork (fun (s) {ignore(send(42, s))}),
             fork (fun (c) {print(intToString(receive(c).1))}));
var y = fuse(fork (fun (c) {print(intToString(receive(c).1))}),
             fork (fun (s) {ignore(send(42, s))}));
()
