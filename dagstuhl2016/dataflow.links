typename Value = [| Const : Int | Unknown |];
typename Label = Maybe (Maybe(Value), Value {}-> ())


# Block : (value -> ()) -> Label
fun block(f) {
  do Block(f)
}

# Jump  : Value * Label -> ()
fun jump(v, l) {
  do Jump(v, l)
}

handler run {
  case Block(f) -> 
  case Jump(v, l) -> 
}
