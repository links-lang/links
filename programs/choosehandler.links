fun positive(m) {
  handle(m) {
    case Choose(k) -> k(true)
    case Return(x) -> x
  }
}

var x = positive(fun() {
  var y = if (do Choose) 0
          else 1;
  var z = if (do Choose) 2
          else 3;
  y + z
});

print(intToString(x))
